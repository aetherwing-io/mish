/// Condense handler — invokes the squasher pipeline.
///
/// PTY capture -> classify -> dedup -> condensed output
pub fn handle(_args: &[String]) -> Result<(), Box<dyn std::error::Error>> {
    todo!("Condense handler not yet implemented")
}
