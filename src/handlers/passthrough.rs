/// Passthrough handler — execute and pass output with metadata footer.
pub fn handle(_args: &[String]) -> Result<(), Box<dyn std::error::Error>> {
    todo!("Passthrough handler not yet implemented")
}
